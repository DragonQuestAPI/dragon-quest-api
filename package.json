{
  "name": "dragon-quest-api",
  "version": "0.1.0",
  "description": "An API to gather informations about the various games in the Dragon Quest series.",
  "main": "src/server.js",
  "scripts": {
    "start": "node .",
    "dev": "nodemon .",
    "sort-json": "node scripts/sort-required-games.js && node scripts/sort-game-names.js",
    "generate-data": "node scripts/generate-data.js",
    "create-structure": "node scripts/generate-server.js && node scripts/generate-readme.js && node scripts/generate-data.js && node scripts/generate-models.js && node scripts/create-default-files.js",
    "test": "jest"
  },
  "keywords": [
    "Dragon Quest",
    "API"
  ],
  "author": "SoruTheWhistler",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.4.5",
    "dragon-quest-api": "file:",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.8.1",
    "@commitlint/config-conventional": "^17.8.1",
    "jest": "^29.7.0",
    "nodemon": "^2.0.22",
    "prettier": "^2.8.8"
  }
}